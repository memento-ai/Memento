{
    "name": "@memento-ai/function-calling",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/function-calling/src",
    "projectType": "library",
    "tags": [],
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "outputs": ["{options.outputPath}"],
            "options": {
                "command": "bun build packages/function-calling/index.ts --outdir dist/function-calling --target node"
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint"
        },
        "test": { "executor": "@nx-bun/nx:test", "options": { "smol": false, "bail": true, "bun": false } },
        "type-check": {
            "executor": "@nx/js:tsc",
            "outputs": ["{options.outputPath}"],
            "options": {
                "outputPath": "dist/out-tsc/packages/function-calling",
                "main": "packages/function-calling/index.ts",
                "tsConfig": "packages/function-calling/tsconfig.json",
                "noEmit": true
            }
        }
    }
}
