{
    "name": "@memento-ai/embedding",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/embedding/src",
    "projectType": "library",
    "tags": [],
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "outputs": ["{options.outputPath}"],
            "options": {
                "command": "bun build packages/embedding/index.ts --outdir dist/embedding --target node"
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint"
        },
        "test": { "executor": "@nx-bun/nx:test", "options": { "smol": false, "bail": true, "bun": false } },
        "type-check": {
            "executor": "@nx/js:tsc",
            "outputs": ["{options.outputPath}"],
            "options": {
                "outputPath": "dist/out-tsc/packages/embedding",
                "main": "packages/embedding/index.ts",
                "tsConfig": "packages/embedding/tsconfig.json",
                "noEmit": true
            }
        }
    }
}
