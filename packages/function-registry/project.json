{
    "name": "@memento-ai/function-registry",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/function-registry/src",
    "projectType": "library",
    "tags": [],
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "outputs": ["{options.outputPath}"],
            "options": {
                "command": "bun build packages/function-registry/index.ts --outdir dist/function-registry --target node"
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint"
        },
        "test": { "executor": "@nx-bun/nx:test", "options": { "smol": false, "bail": true, "bun": false } },
        "type-check": {
            "executor": "@nx/js:tsc",
            "outputs": ["{options.outputPath}"],
            "options": {
                "outputPath": "dist/out-tsc/packages/function-registry",
                "main": "packages/function-registry/index.ts",
                "tsConfig": "packages/function-registry/tsconfig.json",
                "noEmit": true
            }
        }
    }
}
