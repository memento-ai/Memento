{
    "name": "@memento-ai/postgres-db",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/postgres-db/src",
    "projectType": "library",
    "tags": [],
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "outputs": ["{options.outputPath}"],
            "options": {
                "command": "bun build packages/postgres-db/index.ts --outdir dist/postgres-db --target node"
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint"
        },
        "test": { "executor": "@nx-bun/nx:test", "options": { "smol": false, "bail": true, "bun": false } },
        "type-check": {
            "executor": "@nx/js:tsc",
            "outputs": ["{options.outputPath}"],
            "options": {
                "outputPath": "dist/out-tsc/packages/postgres-db",
                "main": "packages/postgres-db/index.ts",
                "tsConfig": "packages/postgres-db/tsconfig.json",
                "noEmit": true
            }
        }
    }
}
