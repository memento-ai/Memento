{
    "name": "@memento-ai/resolution-agent",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/resolution-agent/src",
    "projectType": "library",
    "tags": [],
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "outputs": ["{options.outputPath}"],
            "options": {
                "command": "bun build packages/resolution-agent/index.ts --outdir dist/resolution-agent --target node"
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint"
        },
        "test": { "executor": "@nx-bun/nx:test", "options": { "smol": false, "bail": true, "bun": false } },
        "type-check": {
            "executor": "@nx/js:tsc",
            "outputs": ["{options.outputPath}"],
            "options": {
                "outputPath": "dist/out-tsc/packages/resolution-agent",
                "main": "packages/resolution-agent/index.ts",
                "tsConfig": "packages/resolution-agent/tsconfig.json",
                "noEmit": true
            }
        }
    }
}
